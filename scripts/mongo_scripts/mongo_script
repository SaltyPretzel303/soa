#!/bin/bash

mongo soa_collector_dev --eval '

	db.Sensors.aggregate(
		[
			{ 
				$match: {sensor_name:"sensor_0"}
			},
			{
				$project: {
					sensor_name:1,
					records:1	
				}
			}			
			
		]
	
	).forEach(printjson)

' | egrep --color "timestamp"
















